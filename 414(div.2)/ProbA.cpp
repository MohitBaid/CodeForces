#include<bits/stdc++.h>
using namespace std;
int main()
{
	int a,b,c,i;	scanf("%d %d %d",&a,&b,&c);
	int n;		cin>>n;
	set <int > myset;
	int ans=0;
	/*for(i=1;i<10;i++)
		printf(" %d ",A[i]);
	printf("\n");	*/
	for(int i=1;i<=n;i++)
	{
		int temp;
		scanf("%d",&temp);
		if(temp<=b);
		else if(temp>=c);
		else
			ans+=1;
	}
	cout<<ans<<endl;
}
